// Obsidian-specific randomizer
// Uses SimpleRandomizer for now, could be extended to use Dice Roller plugin

import { SimpleRandomizer } from '@quartermaster/core/utils/SimpleRandomizer';

export class ObsidianRandomizer extends SimpleRandomizer {
	// Future: Could integrate with Dice Roller plugin
	// For now, just use SimpleRandomizer implementation
}
